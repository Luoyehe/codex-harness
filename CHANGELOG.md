# 更新记录

## v1.1.0 — 安全与稳定性修复

保留三种模型源和单管理员自托管方式，集中修复 v1.0.1 后续独立审计确认的问题。本版本包含部署身份与启动器变更；旧实例必须先按[首次迁移说明](deploy/README.md#首次迁移到-v110)停机备份并使用新版脚本修复重装。

### 管理与部署安全

- 管理网关与 Agent/数据后端改为不同系统账户；管理令牌不再位于 `CODEX_HOME`，本机也必须认证。旧实例会生成新管理令牌；应用代码与运行时必须由 root 管理。
- 完全重置改为非 root、目录句柄绑定的安全删除，并核对 Linux mount ID，拒绝跨越同文件系统的绑定挂载。卸载兼容真实项目清单格式，检查全部已注册项目、路径别名和其它实例引用，不可靠时停止自动删除。
- root 文件写入绑定可信父目录和新建文件身份；worker 数据维护先永久降权。检查原始软链入口及所有路径分量，持久化规范运行时路径，拒绝非 root 可修改的代码/工具路径，不自动接管属主。
- 受管 Linux worker 使用 subreaper 和 pidfd 回收完整后台进程树，只有确认子进程全部退出才允许替代后台；清理未知时保留受理账本并阻止新任务，需由 systemd 重启服务恢复。
- 修复 root 事务式更新的测试身份与执行环境：通过 DynamicUser 运行独立临时副本，兼容 `/run` 的 `noexec` 与固定 pnpm 的嵌套调用，不将测试副本写回受信源码；回滚失败保留恢复材料。

### 对话与运行稳定性
- 发送请求使用持久化操作编号；断线或崩溃后未知状态不自动重发。文件附件拥有独立消息元数据；修复多字节文件名和中断清理恢复。
- 发送受理绑定真实会话、操作编号、文本修订和附件快照；跨会话切换、延迟确认及断线恢复不会清空后来编辑的草稿，不确定发送保持锁定，避免重复提交。
- 配置修改、同步和延迟重启与任务启动统一协调；无变化不重启，智谱刷新失败不回退覆盖。自定义新模型不再继承未经确认的能力。
- 管理操作使用独立的持久化编号/结果，重连后核对重启状态；结果未知或脚本未确认退出时阻止重叠操作。移除不再支持的旧式 root HTTPS 网页配置入口。
- 以所选模型能力提供和校验思考档位；跨标签页只提交真正改变的显示偏好；坏项目路径不影响其它项目。
- 自定义模型未声明思考档位时仍可使用原生默认值，显式覆盖则按能力校验；恢复默认会清除上次覆盖，不把默认值错误当作用户指定的档位。
- MCP 两种入口共享增量传输；交互输入、密码和受支持 MCP 表单有真实 UI、共享校验、取消与重连处理。
- 移除基于文本内容猜测的流去重；终端输入有序分块，失败可见且不重试不确定写入；进程清理等待真实退出后才重启。
- 为 socket、stdio、并发调用与历史缓存设置容量保护，慢客户端重新同步；不把截断数据当作完整历史。
- 更新 README 和部署指南，区分入口/管理/模型登录、首次迁移与日常更新，并明确终端权限、凭证保留及验证责任。

### 已知限制与验证边界

本轮循环审计修复了已确认的高优先级问题；最后一轮未再确认新的 P0/P1，**不代表不存在其它缺陷**。按修复范围保留的低优先级问题包括：

- 断线发送审批回复失败时，审批项可能先从界面消失；请检查任务状态后重连，不要把条目消失当作已批准。
- 并发加载多张较大的历史图片时，部分图片可能因忙碌而未显示，需要稍后重新打开。
- 失败回合的部分错误/状态未完整回放；请结合实时提示和服务器日志排查，历史无报错不代表执行成功。
- 多标签页的状态删除同步和错过确认仍可能留下“结果待确认”锁；先保存新草稿、核对历史与操作结果，切勿直接重复发送。
- 工具列表脚本的超时/错误退出、外部 Authelia 的访问策略/健康验证和部分 edge 回滚失败分支仍需人工核对；部分工具长文本显示会截断。列表成功或页面可访问不是业务验收。

修复源码已通过 Windows/Linux 回归、类型检查、构建、脚本语法、ShellCheck、发布内容扫描及依赖审计；Linux 另验证了实际 DynamicUser 候选测试流程。固定 Codex 0.149.0 的隔离初始化与本机模拟 Responses 回归不读取现有凭证、不启动商业模型推理。最终发布提交的自动检查以 GitHub Actions 为准。

此前的供应商实测不等于最终提交重新通过外部业务验收。此发布不自动升级服务器，也未重新调用商业模型或真实 MCP 任务；公网 DNS/ACME、外部防火墙、真实模型/MCP 和外部认证部署仍需在目标环境验证。

## v1.0.1

本次为安全与稳定性修复版本，保留三种模型源、个人自托管和 Linux systemd 部署方式。

### 修复与完善

- “默认”模型、审批、沙箱与思考档位恢复为当前项目/模型源的有效配置，不再沿用上一次覆盖值。
- 修复附件索引异常时的误删风险、失败发送覆盖活跃回合、分页与搜索列表不一致。
- 终端按浏览器连接隔离控制与清理；修复早到输出/退出丢失、尺寸不同步、重启期间创建与 Windows 空格路径启动问题。
- 审批入口保持可见；终端组件按需加载，减少首次页面加载体积。
- 智谱 MCP 桥支持有界流式处理与独立取消；模型目录刷新不覆盖并发修改后的配置。
- 收紧开发代理来源校验和部署权限边界，修复错误实例重置、外部认证服务回滚及认证入口 URL 更新问题。
- 重写 README，区分用户准备事项、自动安装内容、SSH/HTTPS 访问和日常维护。

### 升级前必读

- 服务必须使用非 root 账号。旧 root 服务及 root 所有的环境文件须先明确备份、迁移账号/路径/权限；`ALLOW_ROOT_SERVICE=1` 不再生效，安装器不会自动递归改属主。
- 网页终端在所属浏览器连接断开后回收，不能在重连后接管原 shell；Codex 回合与终端生命周期不同。
- 自定义端点必须支持 Responses API，仅支持 Chat Completions 的端点不能直接使用。
- 共享的外部 Authelia 入口不在受管理路由中时，需要显式提供并验证规范登录 URL。
- 正常 Git 安装使用 `sudo codex-harness update`；旧 root 实例先按[部署指南](deploy/README.md)完成迁移，文件拷贝部署使用修复重装。

### 验证范围

已通过 Windows/Linux 回归、类型检查、构建、脚本检查，以及固定 Codex 0.149.0 的隔离初始化和回环 Responses 默认值恢复验证。默认测试不调用商业模型；公网证书、外部防火墙及真实供应商业务验收须在目标环境另行验证。
